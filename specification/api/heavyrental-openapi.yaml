openapi: 3.0.3
info:
  title: Heavy Rental API
  description: |
    HTTP API for the Heavy Rental mobile operations app (deliveries & returns).

    **v1 mobile client behaviour:**
    - Loads Delivery List via `GET /api/deliveries` and Return List via
      `GET /api/returns` (server/mock applies today membership; see
      specification/domain/list-filters.md).
    - May also load `GET /api/bookings` for shared booking state; does not
      rebuild list screens by re-filtering bookings with device LocalDate.now().
    - Updates status via delivery/return PATCH endpoints. Returns additionally
      accepts a `returnNotes` field on completion (`ReturnStatusUpdateRequest`)
      — deliveries has no equivalent notes field.
    - Auth is an interim → access JWT handshake (getBearerToken → login → logout).
      Contract mirrors heavy-rental-spring-rest-api's
      specification/SPEC-auth-login-logout.md, which is the source of truth for
      server-side behaviour (password verification, denylist, token claims).
      **The local Mockoon mock only returns static canned responses for these
      three routes** — it does not verify credentials or issue real JWTs. Use
      the real Spring Boot backend to exercise actual login/logout behaviour.
  version: 1.0.0

servers:
  - url: http://localhost:8081
    description: Mockoon / Prism mock — host machine (mock generation target)
  - url: http://10.0.2.2:8081
    description: Mockoon / Prism mock — Android emulator → host localhost
  - url: http://localhost:8080
    description: Spring Boot backend — host machine (app default since HR-78)
  - url: http://10.0.2.2:8080
    description: Spring Boot backend — Android emulator → host localhost

tags:
  - name: Auth
  - name: Bookings
  - name: Deliveries
  - name: Returns

paths:
  /api/auth/getBearerToken:
    get:
      tags: [Auth]
      operationId: getBearerToken
      summary: Mint a short-lived interim JWT
      description: |
        Step 1 of login. No credentials required. Response is a raw JWT string
        (`text/plain`), not JSON. See specification/product/01-login.md.

        Full contract owned by heavy-rental-spring-rest-api's
        SPEC-request-bearer-token.md — this mock only returns a fixed
        placeholder token, it does not sign real JWTs.
      responses:
        "200":
          description: Interim JWT (plain text body, not JSON)
          content:
            text/plain:
              schema:
                type: string
                example: eyJhbGciOiJIUzI1NiJ9.mock-interim-token-for-local-mock-testing-only.mock-signature

  /api/auth/login:
    post:
      tags: [Auth]
      operationId: login
      summary: Upgrade an interim JWT to an access (session) JWT
      description: |
        Step 2 of login. Requires the interim JWT from getBearerToken as a
        Bearer token, plus email/password in the body. See
        specification/product/01-login.md and, for full server behaviour,
        heavy-rental-spring-rest-api's SPEC-auth-login-logout.md §6.1.

        **Mock caveat:** this Mockoon route returns the same canned 200
        response for any request body — it does not check the password or the
        interim token. Use the real backend to test 400/401/403 paths.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
            example: "Bearer <interim-jwt>"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Access JWT issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
              examples:
                sample:
                  $ref: "#/components/examples/LoginResponseExample"
        "400":
          description: Blank email or password
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Missing/invalid/expired/reused interim token, or wrong credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: An access token (not an interim token) was used as the Bearer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/auth/logout:
    post:
      tags: [Auth]
      operationId: logout
      summary: Revoke the current access JWT
      description: |
        Step 4 of the flow. Requires the access JWT (from login) as a Bearer
        token. See heavy-rental-spring-rest-api's SPEC-auth-login-logout.md §6.2.

        **Mock caveat:** this Mockoon route always returns 200 — it does not
        validate or actually revoke the Bearer token.
      parameters:
        - name: Authorization
          in: header
          required: true
          schema:
            type: string
            example: "Bearer <access-jwt>"
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
              examples:
                sample:
                  $ref: "#/components/examples/LogoutResponseExample"
        "403":
          description: An interim token (not an access token) was used as the Bearer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/bookings:
    get:
      tags: [Bookings]
      operationId: getBookings
      summary: List all bookings
      description: Primary data load for the v1 Android client.
      responses:
        "200":
          description: Array of bookings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Booking"
              examples:
                sample:
                  $ref: "#/components/examples/BookingList"

  /api/bookings/{bookingId}:
    parameters:
      - $ref: "#/components/parameters/BookingId"
    get:
      tags: [Bookings]
      operationId: getBooking
      summary: Get a single booking
      responses:
        "200":
          description: Booking found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "404":
          description: Booking not found
    put:
      tags: [Bookings]
      operationId: updateBooking
      summary: Replace a booking
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Booking"
      responses:
        "200":
          description: Updated booking
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "404":
          description: Booking not found

  /api/deliveries:
    get:
      tags: [Deliveries]
      operationId: getTodaysDeliveries
      summary: Today's delivery items
      description: |
        Bookings with startDate = today and status CONFIRMED or MOBILISED
        (see specification/domain/list-filters.md). v1 mobile **loads this
        endpoint for the Delivery List and home delivery counts** — it does
        not re-filter the response by device LocalDate.now().
      responses:
        "200":
          description: Today's deliveries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DeliveryItem"
              examples:
                sample:
                  $ref: "#/components/examples/DeliveryList"

  /api/deliveries/{bookingId}/status:
    parameters:
      - $ref: "#/components/parameters/BookingId"
    patch:
      tags: [Deliveries]
      operationId: updateDeliveryStatus
      summary: Update delivery booking status
      description: |
        v1 client uses this for CONFIRMED → MOBILISED.
        Body field bookingStatus must be a valid BookingStatus string.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StatusUpdateRequest"
            examples:
              mobilise:
                value:
                  bookingStatus: MOBILISED
      responses:
        "200":
          description: Updated delivery item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeliveryItem"
        "400":
          description: Invalid transition or body
        "404":
          description: Booking not found

  /api/returns:
    get:
      tags: [Returns]
      operationId: getTodaysReturns
      summary: Today's return items
      description: |
        Bookings with endDate = today and status MOBILISED or COMPLETED
        (see specification/domain/list-filters.md). v1 mobile **loads this
        endpoint for the Return List and home return counts** — it does not
        re-filter the response by device LocalDate.now().
      responses:
        "200":
          description: Today's returns
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReturnItem"
              examples:
                sample:
                  $ref: "#/components/examples/ReturnList"

  /api/returns/{bookingId}/status:
    parameters:
      - $ref: "#/components/parameters/BookingId"
    patch:
      tags: [Returns]
      operationId: updateReturnStatus
      summary: Update return booking status
      description: |
        v1 client uses this for MOBILISED → COMPLETED. Unlike deliveries, the
        request body also carries an operator-entered returnNotes value —
        see ReturnStatusUpdateRequest, a schema dedicated to this endpoint
        rather than the shared StatusUpdateRequest used by deliveries.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnStatusUpdateRequest"
            examples:
              complete:
                value:
                  bookingStatus: COMPLETED
                  returnNotes: Returned in good condition
      responses:
        "200":
          description: Updated return item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReturnItem"
        "400":
          description: Invalid transition or body
        "404":
          description: Booking not found

components:
  parameters:
    BookingId:
      name: bookingId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        example: 3

  schemas:
    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          example: admin@localhost
        password:
          type: string
          example: admin1234

    LoginResponse:
      type: object
      required:
        - accessToken
        - tokenType
        - expiresIn
        - username
      properties:
        accessToken:
          type: string
          description: Session JWT
        tokenType:
          type: string
          example: Bearer
        expiresIn:
          type: integer
          format: int64
          description: Seconds until the access token expires
          example: 3600
        username:
          type: string
          description: Legacy field name — value is the authenticated email
          example: admin@localhost

    MessageResponse:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          example: Logged out successfully

    ErrorResponse:
      type: object
      required:
        - error
        - message
      properties:
        error:
          type: string
          example: invalid_credentials
        message:
          type: string
          example: Invalid email or password.

    BookingStatus:
      type: string
      enum: [PENDING_DEPOSIT, PENDING_CONFIRMED, CONFIRMED, MOBILISED, COMPLETED, CANCELLED]
      description: Hire lifecycle status (see specification/domain/booking-status-machine.md)

    Booking:
      type: object
      description: >
        Nullability follows the backend column definitions (SPEC-entity-repository.md §5.7):
        customer, start_date, end_date, status, site_address and delivery_notes are all
        nullable, so the key is always present but its value may be null. Clients must
        tolerate null rather than failing the whole response. assetName/serialNumber are
        derived by BookingMapper and default to "" when a booking has no items.
      required:
        - bookingId
        - customerName
        - startDate
        - endDate
        - bookingStatus
        - siteAddress
        - assetName
        - serialNumber
        - deliveryNotes
      properties:
        bookingId:
          type: integer
          format: int64
          example: 1
        customerName:
          type: string
          nullable: true
          example: Lim Construction Pte Ltd
        startDate:
          type: string
          format: date
          nullable: true
          description: ISO-8601 date (yyyy-MM-dd)
          example: "2026-08-03"
        endDate:
          type: string
          format: date
          nullable: true
          example: "2026-08-10"
        bookingStatus:
          allOf:
            - $ref: "#/components/schemas/BookingStatus"
          nullable: true
        siteAddress:
          type: string
          nullable: true
          example: 18 Tuas South Ave 14, Singapore 637471
        assetName:
          type: string
          description: Empty string when the booking has no items — see 03-deliveries.md K1
          example: CAT 320 Hydraulic Excavator
        serialNumber:
          type: string
          example: CAT 320 GC
        deliveryNotes:
          type: string
          nullable: true
          example: ""

    DeliveryItem:
      type: object
      required:
        - bookingId
        - customerName
        - startDate
        - siteAddress
        - assetName
        - serialNumber
        - deliveryNotes
        - bookingStatus
      properties:
        bookingId:
          type: integer
          format: int64
        customerName:
          type: string
          nullable: true
        startDate:
          type: string
          format: date
          nullable: true
        siteAddress:
          type: string
          nullable: true
        assetName:
          type: string
          description: One asset only — see 03-deliveries.md K1
        serialNumber:
          type: string
        deliveryNotes:
          type: string
          nullable: true
        bookingStatus:
          allOf:
            - $ref: "#/components/schemas/BookingStatus"
          nullable: true

    ReturnItem:
      type: object
      required:
        - bookingId
        - customerName
        - endDate
        - siteAddress
        - assetName
        - serialNumber
        - deliveryNotes
        - returnNotes
        - bookingStatus
      properties:
        bookingId:
          type: integer
          format: int64
        customerName:
          type: string
          nullable: true
        endDate:
          type: string
          format: date
          nullable: true
        siteAddress:
          type: string
          nullable: true
        assetName:
          type: string
          description: One asset only — see 03-deliveries.md K1
        serialNumber:
          type: string
        deliveryNotes:
          type: string
          nullable: true
          description: Note captured at delivery time — kept for context alongside returnNotes, not replaced by it
        returnNotes:
          type: string
          nullable: true
          description: >
            Note entered by the operator on completion (MOBILISED → COMPLETED).
            Empty/null until the booking is completed. See product/04-returns.md.
          example: Returned in good condition
        bookingStatus:
          allOf:
            - $ref: "#/components/schemas/BookingStatus"
          nullable: true

    StatusUpdateRequest:
      type: object
      description: Used by the delivery status endpoint only — no notes field.
      required:
        - bookingStatus
      properties:
        bookingStatus:
          $ref: "#/components/schemas/BookingStatus"

    ReturnStatusUpdateRequest:
      type: object
      description: >
        Used by the return status endpoint only. Deliberately a separate schema from
        StatusUpdateRequest rather than that type plus an added field — deliveries has
        no use for a notes field, so sharing the type would mean it silently accepts
        one it ignores.
      required:
        - bookingStatus
        - returnNotes
      properties:
        bookingStatus:
          $ref: "#/components/schemas/BookingStatus"
        returnNotes:
          type: string
          description: May be an empty string; there is no way to omit this field.
          example: Returned in good condition

  examples:
    LoginResponseExample:
      summary: Canned access token (mock only — see path description)
      externalValue: ./examples/login-response.json
    LogoutResponseExample:
      summary: Canned logout confirmation (mock only)
      externalValue: ./examples/logout-response.json
    BookingList:
      summary: Sample bookings (fixed date 2026-08-03 as "today")
      externalValue: ./examples/bookings.json
    DeliveryList:
      summary: Sample deliveries
      externalValue: ./examples/deliveries.json
    ReturnList:
      summary: Sample returns
      externalValue: ./examples/returns.json
